const ESC_KEY = `Escape`;

const DateFormat = {
  comment: `YYYY/MM/DD HH:mm`,
  release: `DD MMMM YYYY`,
};

const FilterType = {
  ALL: `all`,
  WATCHLIST: `watchlist`,
  HISTORY: `history`,
  FAVORITES: `favorites`,
};

const SortType = {
  DEFAULT: `default`,
  BY_DATE: `by-date`,
  BY_RATING: `by-rating`,
};

export {
  ESC_KEY,
  DateFormat,
  FilterType,
  SortType,
};
